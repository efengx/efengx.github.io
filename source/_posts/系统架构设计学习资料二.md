---
title: 系统架构设计学习资料二
date: 2019-10-10 11:05:11
tags: [系统架构]
---

学习资料，慎入！！！
<!-- more -->

### 软件架构风格分类

#### 数据流风格

### 调用/返回风格

调用/返回实际是一种分而治之的策略，其主题思想是将一个大系统分解成多个不同的子系统，以便降低复杂性，并增加可修改性。主要有以下三种架构设计风格：

1. 主程序/子程序
这是一种在结构化开发时期的经典架构风格。这种风格一般采用单线程控制，把问题分解成若干个处理步骤，构件即为主程序和子程序，过程调用作为交互机制，即充当连接件。

2. 面向对象风格
抽象数据概念对软件系统有着重要作用，目前软件届以普遍使用面向对象系统。这种风格建立在数据抽象和面向对象的基础上，数据的表示方法和他们响应的操作被封装抽象数据类型或对象中。构件即为对象，或是说是抽象数据类型的实例。对象是一种被称作管理者的构件，因为它负责保持资源的完整性。对象是通过函数和过程的调用来交互的。
这种风格有两个主要的特征：
    - 对象负责维护其表示的完整性。
    - 对象的表示对其他对象而言是隐藏的，因为一个对象对其它的客户隐藏了自己的表示，所以这些对象可以不影响其他客户就能改变其实现方法。
面向对象系统也存在着某些问题：
    - 为了使一个对象与另一个对象通过过程调用等进行交互，必须知道对象的标示。只要一个对象的标示发生了改变，就必须修改所有其他明确调用它的对象。
    - 必须修改所有显示调用它的对象，并消除由此带来的一些副作用。例如A使用了对象B,C也使用了对象B,那么C对B的使用所造成的对A的影响可能是料想不到的。

3. 层次结构风格
层次结构系统组成了一个层次结构，每一层为上层服务，作为下层的客户。在一些层次系统中，除了一些精心挑选的输出函数外，内层的层只对相邻层可见。这样的系统中构件在一些层形成了虚拟机。连接件通过决定层间如何交互的协议来定义，拓扑约束包括对相邻层间的交互约束。
这种风格基于可增加抽象层的设计。这样允许将一个复杂的问题分解成一个增量步骤序列的实现。由于每一层最多只影响两层，同时只要给相邻层提供相同的接口，允许每层用不同的方法实现，同样为软件重用性提供了强大的支持。

#### 独立构件设计风格

独立构件设计风格主要强调系统中的每个构件都是相对独立的个体，它们之间不直接通信，以降低耦合度。提升灵活性。独立构件风格主要包括：进程通信和事件系统风格

1. 进程通信架构风格：构件是独立的进程，连接件是消息传递。这种风格的特点是构件通常是命名过程，消息传递的方式可以是点对点、异步方式和同步方式及远程过程调用等。
2. 事件系统风格：这种风格的思想是构件不直接调用一个过程，而是触发或广播一个事件。系统中的其他构件在一个或多个事件中注册，当一个事件被触发时，系统自动调用这个事件中注册的所有过程。这样一个事件的触发就导致另一个模块中的过程被调用。

#### 虚拟机风格

---

#### 企业服务总线

ESB的概念是从SOA发展而来的，他是一种为进行连接服务提供标准化的通信基础结构，基于开放的标准，为应用提供一个可靠的、可度量的和高度安全的环境，并可帮助企业对业务流程进行设计和模拟，对每个业务流程进行控制和跟踪、分析并改进流程和性能。

#### 微服务

它是属于面向服务的一种，微服务的特点：小，且专注于做一件事情、轻量级的通信机制、松耦合、独立部署。
微服务的优势：

1. 技术异构性
2. 弹性
3. 扩展
4. 简化部署
5. 与结织结构相匹配：微服务架构可l以将架构与组织结构相匹配，避免出现过大的代码库。
6. 可组合性
7. 对可替代性的优化

微服务面临的挑战：（软件开发业内有一句名言“软件开发没有银弹”；简单理解，软件开发业内没有一种方法可以解决所有的问题）

1. 分布式系统的复杂度
2. 运维成本
3. 部署自动化
4. DevOps与组织结构
5. 服务间依赖测试
6. 服务间依赖管理

### 架构设计

## 测试评审方法

### 测试方法

首先需要明确“错误”（error）和“缺陷”（fault）的概念。
IEEE的定义：“错误”主要是针对软件开发过程，“缺陷”主要是针对软件产品。软件开发人员在软件开发过程（主要是分析、设计和编码过程）中所出现的“错误”是导致软件产品“缺陷”的原因，反过来说，“缺陷”是“错误”的结果和表现形式。
为了发现软件中的错误（缺陷），应竭力设计能暴露错误（缺陷）的测试用例。
测试用例是由测试数据和预期结果构成的。
一个好的测试用例极有可能发现至今为止尚未发现的错误（缺陷）的测试测试用例。
一个成功的测试是发现了至今为止尚未发现的错误（缺陷）的测试。
高效的测试是指用少量的测试用例，发现被测软件尽可能多的错误（缺陷）。
软件测试所追求的目标就是以尽可能少的时间和人力发现软件产品中尽可能多的错误（缺陷）。

#### 软件测试阶段

1. 单元测试（也称模块测试）
由程序员对自己编写的模块自行测试，检查模块是否实现了详细设计说明书中规定的功能和算法。
单元测试计划应该在详细设计阶段制定。
单元测试重点：模块接口、局部数据结构、重要执行通路、出错处理通路和边界条件等。
测试一个模块时需要为该模块编写一个驱动模块和若干个桩（stub）模块。
2. 集成测试（也称组装测试）

3. 系统测试（）

### 评审方法

### 验证与确认

### 测试自动化

### 面向对象的测试
